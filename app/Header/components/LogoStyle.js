import styled from 'styled-components';

const LogoStyle = styled.div`
     float: left;
     color: #524c49;
     font-weight: 500;
     font-size: 35px;
`;

const Highlight = styled.span`
     color: #f99f6f;
`;

export { LogoStyle, Highlight };